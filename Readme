# Generate Semantically Related Questions from PDF (Using Google Gemini)

## Overview
This Streamlit application allows users to upload a PDF containing questions and generate semantically related questions using the **Google Gemini API**. The application uses `pdfplumber` to extract text, regex to detect questions, and the **Google Gemini Pro** AI model to generate related questions based on the input.

---

## Features
- **PDF Text Extraction**:
  Extract text from uploaded PDF files using `pdfplumber`.
- **Question Detection**:
  Identify questions in the text using regex.
- **Semantic Question Generation**:
  Use the Google Gemini API to generate semantically related questions.
- **Interactive Interface**:
  Built with Streamlit for user-friendly interaction.

---

## Requirements

### Prerequisites
- Python 3.8 or higher
- Google Gemini API access
- Environment variable setup for API key (`GOOGLE_API_KEY`)

### Python Libraries
Install the required libraries using the following command:
```bash
pip install streamlit pdfplumber google-generativeai langchain python-dotenv nltk
